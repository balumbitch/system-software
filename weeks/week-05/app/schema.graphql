type Notification {
  id: ID!
  name: String!
  channel: String!
}

type Query {
  notifications: [Notification!]!
}

type Mutation {
  createNotification(name: String!, channel: String!): Notification!
}
